[project]
name = "sqlite-mcp"
version = "0.1.0"
description = "MCP server for SQLite databases"
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=1.26.0",
]

[project.scripts]
sqlite-mcp = "sqlite_mcp.server:main"
